{
  "name": "kc1010",
  "version": "1.0.0",
  "private": true,
  "description": "Система контроля качества звонков КЦ1010",
  "scripts": {
    "dev": "node scripts/dev-server.js 3000",
    "dev:next": "next dev",
    "build": "next build",
    "start": "next start",
    "db:init": "psql -d $PGDATABASE -c 'CREATE EXTENSION IF NOT EXISTS citext;' -f db/schema.sql",
    "db:migrate": "node scripts/migrate.js",
    "test": "bash test/run.sh"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "next": "^15.0.0",
    "pg": "^8.23.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
