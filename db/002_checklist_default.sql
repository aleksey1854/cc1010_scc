-- ============================================================
-- Значение пункта по умолчанию.
--
-- В оригинальной таблице чек-лист открывается уже заполненным:
-- «положительно», а где уместно — «не требуется», события «Не обнаружено».
-- Поэтому лист начинается со 100% и опускается по мере отметки ошибок.
-- Без этой колонки интерфейс не знал, что подставлять.
-- ============================================================

ALTER TABLE checklist_items
  ADD COLUMN IF NOT EXISTS default_value answer_value;
