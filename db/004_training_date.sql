-- ============================================================
-- 004: дата начала обучения оператора
--
-- Стаж считается от даты приёмки (hired_at), но заводят человека
-- раньше — в день выхода на обучение. Приёмку вносят позже, и до
-- этого момента стажа у оператора ещё нет.
-- ============================================================
ALTER TABLE staff ADD COLUMN IF NOT EXISTS training_at date;
