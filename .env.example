# Neon: Dashboard → Connection string → Pooled connection
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/kc1010?sslmode=require

# Размер пула. На serverless держим узким: каждая функция — свой процесс.
PG_POOL_MAX=3

# --- только для сверки паритета со старым проектом на Apps Script ---
# Путь к его харнессу. Без переменной parity.js и parity-api.js пропускаются.
# OLD_PROJECT=../qc/test/harness
