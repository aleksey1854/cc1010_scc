# КЦ1010 на Postgres + Vercel

Перенос системы контроля качества с Google Apps Script.
Цель — **паритет**: те же цифры, те же формы ответов, никаких новых функций.

## Тесты

```bash
node scripts/seed.js --write          # демо-выгрузка, если боевой нет
ENV_FILE=.env.test bash test/run.sh
```

Ожидаемо: **98 проверок, 0 расхождений**.

**Тесты пересоздают схему и стирают данные.** Отдельный `.env.test` с другой
базой — не прихоть: с `.env` прогон снесёт то, что лежит в рабочей.

| тест | что делает |
|---|---|
| `parity.js` | ядро против Apps Script: 5000 чек-листов, 1826 дат, хеши в обе стороны |
| `migration.js` | данные после переноса совпадают с выгрузкой |
| `parity-api.js` | обе системы на одних данных, ответы сверяются полем в поле |
| `chain.js` | сквозной сценарий: вход, заявка, оценка, кабинеты, отчёты, история |

`parity.js` и `parity-api.js` требуют рядом старый проект на Apps Script
(`OLD_PROJECT`). Без него они не падают, а честно пропускаются.

`dump.json` — выгрузка листов: `{ "Сотрудники": [[...]], "Журнал оценок": [[...]], ... }`.

## Что где

```
db/schema.sql      14 таблиц (слепок с нуля)
db/001_*.sql       изменения схемы поверх слепка
lib/core.js        расчёт балла, даты, хеши, валидация — перенесено без изменений
lib/db.js          доступ к данным, транзакции вместо блокировок
lib/auth.js        сессии и права (заменяют CacheService)
lib/reports.js     кабинеты и отчёты на SQL
lib/api.js         37 обработчиков — имена и формы как в Apps Script
app/api/[fn]/      единственный маршрут Next.js: POST /api/<функция>
public/index.html  интерфейс, перенесён из Apps Script без правок разметки
public/gs-shim.js  подмена google.script.run на fetch
public/ui-select.js списки выбора с поиском поверх родного <select>
public/ui-phone.js  номер телефона в виде +7 (777) 777-77-77
scripts/migrate.js перенос выгрузки
scripts/seed.js    демо-выгрузка, чтобы репозиторий заводился без боевых данных
scripts/db-reset.js чистая схема на месте (psql не нужен)
```

## Правила, которые держит сервер

Проверки стоят и на странице, и в `lib/db.js`: API открыт наружу, и
полагаться на интерфейс нельзя.

- оценку нельзя сохранить с незаполненным чек-листом — обязательны все
  23 балльных пункта, события отмечаются по факту;
- описание звонка обязательно целиком: время, телефон, критерий, тематика,
  город, а подтематика — там, где она заведена в справочнике;
- повторная оценка одного звонка и дубль заявки запрещены уникальным
  индексом, а не проверкой в коде;
- каждый шаг заявки пишется в `request_events`: создана, отредактирована,
  сменила статус, оценена — с автором и временем.

## Как подключён фронт

`index.html` перенесён без правок разметки: поменялся только транспорт.

```js
// было
google.script.run.withSuccessHandler(cb).getRgoDashboard(pin, period);

// стало
fetch('/api/getRgoDashboard', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ args: [pin, period] })
}).then(r => r.json()).then(cb);
```

Имена функций и формы ответов совпадают, поэтому обработчики не тронуты. Все 28 вызовов
в разметке остались прежними: `public/gs-shim.js` подключается первым и подменяет
`google.script.run` на `fetch`.

## Решения, которые стоит знать

**Хеши паролей совместимы.** Формат `sha256$1200$соль$хеш` считается одинаково в Apps Script и в Node — проверено в обе стороны. 196 человек входят прежними логинами и паролями.

**Блокировок нет.** Девять точек записи, которые закрывались `withLock_`, защищены транзакциями и уникальными индексами. Дубль заявки и повторная оценка звонка запрещены схемой, а не проверкой в коде.

**Храним только отклонения.** «Положительно» — значение по умолчанию, отсутствие строки означает, что пункт выполнен. Убирает 76% строк ответов: 2564 → 1105 байт на оценку.

**Русские подписи живут в интерфейсе.** В базе — коды `pos/dbt/neg/na/yes/no`, перевод происходит в `lib/api.js` на границе. Фронт получает привычные «Положительно».

## Объём и скорость

Замерено на 20 000 оценок в настоящем Postgres:

| | |
|---|---|
| вес оценки | 1105 байт (в Таблицах 928) |
| за 5 лет при 250 оц./день | 0,32 ГБ — влезает в бесплатный Neon |
| за 5 лет при 420 оц./день | 0,54 ГБ — нужен Launch |
| дашборд группы | 3 мс |
| дивизион | 9 мс |
| Отчёт КК | 6 мс |
| критерии по 27 пунктам | 128 мс |

## Деплой: Neon + Vercel

1. **Neon.** Создать проект и в нём базу `kc1010`. Строку подключения брать **Pooled**:
   serverless открывает много коротких соединений, прямая строка их не переживёт.
2. **Схема.** В SQL Editor выполнить `CREATE EXTENSION IF NOT EXISTS citext;`, затем
   содержимое `db/schema.sql`.
3. **Данные.** `DATABASE_URL=... node scripts/migrate.js dump.json`.
   Боевой `dump.json` — выгрузка листов; для проверки на пустом месте подойдёт
   `node scripts/seed.js --write`.
4. **Vercel.** Import Git Repository, фреймворк определится сам (Next.js).
   В Settings → Environment Variables добавить `DATABASE_URL` (та же pooled-строка)
   и `PG_POOL_MAX=3`. Deploy.

После деплоя `/` отдаёт интерфейс, а [`/api/health`](https://cc1010-scc.vercel.app/api/health)
показывает, дошла ли строка подключения до функции и в какую базу она смотрит:

```json
{"databaseUrl":"задан","host":"ep-…-pooler…","pooled":true,"db":"neondb","connect":"ок","staff":72}
```

`"НЕ ЗАДАН"` — переменной в этом окружении нет или она пустая; `"pooled":false` —
строка без `-pooler`, на serverless соединения быстро кончатся. Значение
переменной наружу не отдаётся, только хост и имя базы.

`vercel.json` держит функции в регионе `fra1` — там же, где база. Без этого
они считаются в Вашингтоне, и каждый запрос к базе летает через океан.
Время на сервере — московское (`TZ`), иначе ночные заявки уезжали бы днём назад.

## Локально

```bash
npm install
npm run dev          # свой сервер на scripts/dev-server.js, порт 3000
npm run dev:next     # то же через Next
```

Обоим нужен `DATABASE_URL` в окружении.
